[core]
  autocrlf = input
  longpaths = true
  symlinks = true
[color]
  ui = auto
[column]
  ui = auto
[init]
  defaultBranch = main
[commit]
  verbose = true
[push]
  default = simple
  autoSetupRemote = true
[pull]
  rebase = false
[rebase]
  autosquash = true
  autostash = true
  updateRefs = true
[tag]
  sort = -version:refname
[diff]
  algorithm = histogram
  colorMoved = default
  colorMovedWS = allow-indentation-change
  renames = true
  mnemonicPrefix = true
[merge]
  tool = nvimdiff2
  conflictstyle = zdiff3
[mergetool]
  keepBackup = false
  writeToTemp = true
[rerere]
  enabled = true
  autoupdate = true
[branch]
  # `simple` only sets an upstream when the remote branch has the same name, so
  # branching off `origin/main` leaves the upstream unset for push.autoSetupRemote
  # to fill in on first push. `true` would track `origin/main`, which makes
  # push.default=simple refuse the push and status/pull compare against main.
  autoSetupMerge = simple
  sort = -committerdate
[alias]
  a = add
  ap = add --patch .
  br = branch
  c = commit
  cl = clean -d --force
  co = checkout
  d = diff
  default-branch = !git remote show origin | grep 'HEAD branch' | cut -d' ' -f5
  ds = diff --staged
  f = fetch
  l = log --color --graph --date=format:'%Y-%m-%d %H:%M:%S' --pretty=format:'%Cred%h%Creset %Cgreen%ad%Creset %C(bold blue)%an%Creset %s%C(yellow)%d%Creset'
  m = merge
  mt = mergetool
  n = switch --create
  p = pull
  pl = pull
  praise = blame
  ps = push
  psf = push --force-with-lease
  psu = !git push --set-upstream origin $(git branch --show-current)
  r = restore
  rb = rebase
  root = rev-parse --show-toplevel
  rs = restore --staged
  s = status --short
  st = stash
  sts = stash --staged
  stu = stash --keep-index
  sw = switch
  sy = pull origin main:main --no-edit
  t = tag --no-column --format='%(color:yellow)%(refname:short)%(color:reset) %(color:green)%(creatordate:format:%Y-%m-%d %H:%M:%S)%(color:reset) %(color:bold blue)%(taggername)%(authorname)%(color:reset) %(color:white)%(contents:subject)%(color:reset)'
  wt = worktree
[help]
  autocorrect = 12  # 1.2 seconds
