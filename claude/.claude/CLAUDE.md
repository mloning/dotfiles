In Plan Mode, when saving the plan to a file, always use a descriptive kebab-case filename that summarizes the plan (e.g. `add-user-auth.md`, `migrate-to-postgres.md`), not a random auto-generated name.
